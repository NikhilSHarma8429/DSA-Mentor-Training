class Solution:
    
    def checkIsAP(self, arr, n):
        arr.sort()
        a=arr[1]-arr[0]
        c=0
        for i in range(0,len(arr)-1):
            if((arr[i+1]-arr[i])!=a):
                return False
        return True
                
